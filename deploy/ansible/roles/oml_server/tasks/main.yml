---

#----------------------------------------------------------------------
# Deploy de la app
#
# Tags mas utiles:
#  - virtualenv: setup de virtualenv & requirements
#  - deploy: deploy de la APP (EXCEPTUANDO cambios en otros
#       compoenentes)

#----------------------------------------------------------------------

- include: deploy.yml
  tags:
    - postinstall
    - deploy
    - omniapp

- include: celery.yml
  tags: ['postinstall','celery']
  when: ansible_os_family == "RedHat"
