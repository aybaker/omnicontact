
Aqui estan los archivos relacionados con deploys, ya sean en servidores
reales, docker, etc.

Docker
======

Para utilizar Docker/Fig, es necesario crear un virtualenv en este
directorio. Para crearlo, ejecutar:

    $ virtualenv virtualenv-fig
    $ ./virtualenv-fig/bin/pip install -r requirements.txt
